<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lỗi - Chi tiết đơn hàng</title>
    <!-- Link Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Link custom CSS -->
    <link href="/css/styles.css" rel="stylesheet">
</head>
<body>
    <!-- Banner -->
    <section class="banner-area organic-breadcrumb">
        <div class="container">
            <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
                <div class="col-first">
                    <h1>Chi tiết đơn hàng</h1>
                    <nav class="d-flex align-items-center">
                        <a href="/home">Trang chủ<span class="lnr lnr-arrow-right"></span></a>
                        <a href="/order/list">Danh sách đơn hàng<span class="lnr lnr-arrow-right"></span></a>
                        <a href="/order/detail/{{order._id}}">Chi tiết</a>
                    </nav>
                </div>
            </div>
        </div>
    </section>

    <!-- Error Content -->
    <div class="container container-error">
        <div class="error-banner text-center">
            <h1 class="display-4">Đã xảy ra lỗi</h1>
            <p class="fs-5"><strong>Mã lỗi:</strong> <span class="text-danger">{{errorCode}}</span></p>
            <p class="fs-5"><strong>Thông báo:</strong> <span>{{message}}</span></p>
            {{#if errorDetails}}
            <p class="fs-5"><strong>Chi tiết:</strong> <span>{{errorDetails}}</span></p>
            {{/if}}
            <a href="/order/checkout" class="btn btn-danger btn-back">Quay lại trang Checkout</a>
        </div>
    </div>

    <!-- Link Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
